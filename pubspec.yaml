name: aonego9_vendor
description: AOneGo9 Vendor app — manage profile, services, talent roster, bookings, calendar, earnings & messages.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^14.2.0
  provider: ^6.1.2
  intl: ^0.19.0
  fl_chart: ^0.68.0
  google_fonts: ^6.2.1        # Fraunces/Syne editorial type (shared w/ user app)
  http: ^1.2.2
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/
